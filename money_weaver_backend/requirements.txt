amqp==5.3.1
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
apscheduler==3.11.0
backoff==2.2.1
billiard==4.2.1
blinker==1.9.0
celery==5.5.3
certifi==2025.8.3
cffi==1.17.1
charset-normalizer==3.4.3
click==8.2.1
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
cryptography==45.0.7
dnspython==2.7.0
email-validator==2.3.0
fastapi==0.116.1
Flask==3.1.1
Flask-SQLAlchemy==3.1.1
greenlet==3.2.4
httpx==0.28.1
feedparser==6.0.14
pytrends==4.9.2
idna==3.10
itsdangerous==2.2.0
Jinja2==3.1.6
kombu==5.5.4
MarkupSafe==3.0.2
minio==7.2.16
orjson==3.11.3
packaging==25.0
prompt_toolkit==3.0.52
pycparser==2.22
PyJWT==2.10.1
pycryptodome==3.23.0
python-dateutil==2.9.0.post0
python-dotenv==1.1.0
python-multipart==0.0.20
redis==6.4.0
requests==2.32.5
six==1.17.0
SQLAlchemy==2.0.41
alembic==1.19.1
starlette==0.47.3
typing_extensions==4.14.0
tzdata==2025.2
tzlocal==5.3.1
urllib3==2.5.0
uvicorn==0.35.0
vine==5.1.0
wcwidth==0.2.13
Werkzeug==3.1.3
boto3==1.38.13
numpy==2.2.6 
# LiteLLM proxy dependencies
# NOTE: the LiteLLM PROXY runs from this same venv (see start_all_services.sh,
# which activates `venv` and launches `litellm --config ... --port 8000`).
# Only the client SDK is strictly needed here for src/services/llm_service.py
# and the FastAPI api-keys router, but the proxy runs from the same env.
# Installing litellm[proxy] here would conflict with
# boto3==1.38.13 (proxy pins boto3==1.36.0), cryptography==45.0.7 (proxy wants
# <44.0.0) and fastapi==0.116.1 (proxy wants <0.116.0) — same pattern as the
# torch deps that tts_service owns.
litellm==1.76.1
prisma==0.15.0
tomlkit==0.13.3
nodeenv==1.9.1
websockets==13.1
# Additional dependencies for proper LiteLLM operation
email-validator==2.3.0
python-multipart==0.0.20
# Database dependencies (optional - for LiteLLM proxy if needed)
# 2.9.9 ships no cp313 wheel; 2.9.10+ is the first version with Python 3.13 support
# (avoids forcing a libpq-dev sdist build in CI).
psycopg2-binary>=2.9.10
# edge-tts is LGPL-3.0 — commercial use permitted as unmodified pip dependency;
# pending owner sign-off.
edge-tts==7.2.1
# --- Optional: Chatterbox voice cloning (MIT) ---
# Heavy (torch/torchaudio). Install manually on workers that need cloning:
#   pip install chatterbox-tts==0.1.7   # verify latest via `pip index versions chatterbox-tts`
# Then set CHATTERBOX_ENABLED=true.
# --- Optional: fal.ai voice/video generation (fal-client, Apache-2.0) ---
#   pip install fal-client==1.0.1   # verify latest via PyPI before enabling
# Per-user keys via API keys page (provider 'fal') or env FAL_KEY.
# --- DEV NOTE: MOSS-TTS inference microservice ---
# The TTS microservice lives in money_weaver_backend/tts_service/ and runs in its
# OWN venv (tts_service/.venv, python 3.12). Its deps (torch==2.2.2, torchaudio==2.2.2,
# onnxruntime, soundfile, numpy<2, fastapi, uvicorn, ...) are pinned in
# tts_service/requirements.txt. Do NOT install them into this main venv (Python 3.13).
# See .superpowers/sdd/task-1-report.md and docs/superpowers/plans/2026-08-10-04-phase3-voice-cloning.md.

pyyaml==6.0.2

# --- Smart 9:16 reframe (Task 6) — worker-only ML deps ---
# mediapipe pulls its own opencv; the headless build avoids GUI libs on
# worker images. NOTE: ultralytics is AGPL-3.0 and is FORBIDDEN in this
# repo — TRACK mode degrades to GENERAL until an alternative is chosen.
mediapipe==0.10.21
opencv-python-headless==4.12.0.88

# --- Viral moment detection (Task 7) — lazy-imported worker deps ---
# scenedetect: BSD-3-Clause; google-genai: Apache-2.0 (both license-safe).
# faster_whisper (MIT) is lazy-imported by viral_detector; pinned for
# reproducibility. Install is optional at runtime but required for viral clips.
faster-whisper==1.2.1
scenedetect==0.7.1
google-genai==2.19.0

# --- YouTube private upload (Task 8) — lazy-imported worker deps ---
# Both Apache-2.0 (license-safe). Absence is tolerated at runtime: the
# uploader raises a clear error only when a YouTube feature is used.
google-api-python-client==2.167.0
google-auth-oauthlib==1.2.2

# --- DEV/TEST dependencies ---
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0